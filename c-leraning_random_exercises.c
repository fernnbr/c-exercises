// Create the function "count_uppercase" Return the number of uppercase letters in the string
int count_uppercase(const char *str);

// Return 1 if the string contains at least one digit
int contains_digit(const char *str);

// Returns 1 if all characters are letters (a-z or A-Z), 0 otherwise
int ft_str_is_alpha(const char str);

// Returns 1 if all characters are digits ('0' to '9')
int ft_str_is_numeric(const char *str);

// Returns 1 if all letters are lowercase
int ft_str_is_lowercase(const char *str);

// Returns 1 if all letters are uppercase
int ft_str_is_uppercase(const char *str);

// Counts how many times a given character appears in a string
int ft_count_char_occurrences(const char *str, char target);

// Returns 1 if string is a palindrome (reads same forward and backward)
int ft_is_palindrome(const char *str);

// Returns the number of alphanumeric characters in the string
int ft_count_alpha_numeric(const char *str);










